"use strict";

//declare using the keyword 'const'
//value once assigned, cannot be changed
const pi = 3.14;
//pi = 3.15;   <- This causes an error

let radius = 5;

console.log("Area: "+(pi*(radius**2)));    //Area of circle: pi*r*r